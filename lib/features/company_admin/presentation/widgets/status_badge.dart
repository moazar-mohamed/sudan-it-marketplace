import '../../../../core/widgets/app_widgets.dart';

/// Company Admin name for the shared [StatusChip]; the colour comes from the
/// status's [AppTone].
class StatusBadge extends StatusChip {
  const StatusBadge({super.key, required super.label, required super.tone});
}
